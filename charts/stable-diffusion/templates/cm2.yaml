apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "stable-diffusion.fullname" . }}-cm2
  labels:
    io.kompose.service: diffusion
  {{- include "stable-diffusion.labels" . | nindent 4 }}
  annotations:
    use-subpath: "true"
data:
  auth: {{ .Values.cm2.auth | toYaml | indent 1 }}